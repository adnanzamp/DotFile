require "nvchad.options"

-- Hybrid line numbers: absolute number on the cursor line, relative on the
-- rest. NvChad already sets number=true; we just turn relativenumber on.
vim.opt.relativenumber = true
